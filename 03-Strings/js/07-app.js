const producto = 'monitor 20 pulgadas';


// .toUpperCase para transformar a mayuscula un string

console.log(producto.toUpperCase());

// .toLowerCase para transformar a mayuscula un string

console.log(producto.toLowerCase());

// ejemplo

const precio = 300;
console.log(precio);
console.log(precio.toString());


//ejemplo
const numeroMinusculo = 'teclado gamer';

console.log(`El siguiente resultado muestra ${numeroMinusculo} en mayuscula: ${numeroMinusculo.toUpperCase()}`);
