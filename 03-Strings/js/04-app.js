//Eliminar espacios en blanco en los strings
const _productoo = '           monitor 20 pulgadas          ';

console.log(_producto.length);

// Eliminar del incio....
console.log(_producto.trimStart());


// Eliminar del final
console.log(_producto.trimEnd());

// Eliminar todo los espacios

console.log(_producto.trimStart().trimEnd());
console.log(_producto.trim());
